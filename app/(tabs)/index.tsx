import { getMachines } from "@/service/FirebaseService";
import { getAllUserNotifications } from "@/service/Notification";
import React, { useEffect } from "react";
import { Pressable, Text, View } from "react-native";

export default function HomeScreen() {
  
  return (
    <View style={{ flex: 1 }}>
      
    </View>
  );
}
